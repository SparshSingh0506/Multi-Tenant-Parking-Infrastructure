import type { Context } from "hono";

export const adminRegisterController = (c: Context) => {

  return c.json({

  });
}

export const adminLoginController = (c: Context) => {

  return c.json({ 

  });
}
