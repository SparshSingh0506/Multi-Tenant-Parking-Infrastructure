import type { ManagerRegister } from "@/schemas/auth.schema.js";

import { env } from "../configs/env.config.js";

export const managerRegisterService = async (manager: ManagerRegister) => {
  return {}
}


export const managerLoginService = async (email: string, password: string) => {

}
